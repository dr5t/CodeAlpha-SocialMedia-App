services:
  - type: web
    name: vibe-social
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: DATA_DIR
        value: /data
    disk:
      name: vibe-data
      mountPath: /data
      sizeGB: 1
